import React from 'react';

export const StyleAdvanced = (): JSX.Element => {
  const [show, setShow] = React.useState(false);

  return (
    <div>
      <button>Advanced options</button>
    </div>
  );
};
